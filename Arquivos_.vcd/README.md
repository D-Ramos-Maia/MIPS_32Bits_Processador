## Como foram gerados.

Todos arquivos aqui gerados foram gerados com a ferramenta `gtkwave`. Usando como base os arquivos encontrados em nas pastas `testbench ` e `componente `. Caso queira refazer os arquivos aqui descritos, devem retirar os comentários dos trechos dos códigos dos testbench que geram os arquivos `.vcd`.
