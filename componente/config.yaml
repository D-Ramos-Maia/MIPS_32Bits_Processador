# LibreLane configuration file
DESIGN_NAME: mips_top
VERILOG_FILES: dir::*.sv
CLOCK_PORT: clk
CLOCK_PERIOD: 10

# Corrigindo o erro de "string to list": áreas devem ser listas [x0, y0, x1, y1]
DIE_AREA: [0, 0, 500, 500]
CORE_AREA: [40, 40, 460, 460]

# Removendo o prefixo 'FP_' conforme os avisos de depreciação
PDN_VOFFSET: 10
PDN_HOFFSET: 10
PDN_VPITCH: 80
PDN_HPITCH: 80

# Utilização do core
FP_CORE_UTIL: 20
